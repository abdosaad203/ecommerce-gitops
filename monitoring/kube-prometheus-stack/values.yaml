grafana:
  adminPassword: admin123

prometheus:
  prometheusSpec:
    retention: 7d

alertmanager:
  enabled: true

prometheusOperator:
  admissionWebhooks:
    enabled: false

  tls:
    enabled: false
