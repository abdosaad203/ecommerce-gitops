apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: identity-api-monitor
  namespace: monitoring

spec:
  selector:
    matchLabels:
      app: identity-api

  namespaceSelector:
    any: true

  endpoints:
    - port: http
      path: /metrics
      interval: 15s