grafana:
  enabled: true

  adminPassword: admin123

  resources:
    requests:
      cpu: 50m
      memory: 64Mi

prometheus:
  enabled: true

  prometheusSpec:
    retention: 1d

    resources:
      requests:
        cpu: 100m
        memory: 128Mi

alertmanager:
  enabled: false

kube-state-metrics:
  enabled: true

prometheus-node-exporter:
  enabled: true