apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: order-api-monitor
  namespace: monitoring

spec:
  selector:
    matchLabels:
      app: order-api

  namespaceSelector:
    any: true

  endpoints:
    - port: http
      path: /metrics
      interval: 15s